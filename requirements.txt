# AtlasVPN — interfaz y logos
customtkinter>=5.2.0
Pillow>=10.0.0

# UI web (python -m atlasvpn) + ventana de escritorio sin navegador externo
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
pywebview>=5.0

# Cliente oficial Cloudflare (útil para scripts/API; la sincronización de AtlasVPN usa REST v4 con el mismo token)
cloudflare>=3.1.0
